# ALL-SKILLS AUTONOMOUS ENGINE & PERSISTENT ARCHITECTURE DIRECTIVES

All agents and subagents operating within `d:/Motorent` MUST simultaneously adhere to:
1. **Server Modular Monolith Architecture**: Strictly follow `src/config`, `src/core` (middlewares), `src/models` (18 tables), `src/modules` (feature-based controllers & routes), and `src/routes/index.ts`.
2. **STRICT SVG ONLY RULE**: ZERO emoji in UI. ZERO font icons. ONLY use pure vector SVG / inline SVG components.
3. **Superpowers & GStack Quality Gates**: Verify before declaring completion, run health checks.
4. **UI/UX Pro Max & Taste Skill**: Premium aesthetics, curated color schemes, zero placeholders.
5. **Domain Modeling & Matt Pocock Guidelines**: Clean architecture, strong types, no regressions.
6. **Ponytail & Caveman**: Concise, high-density output.
7. **Hermes Agent & Last30Days**: Autonomous, up-to-date execution.
